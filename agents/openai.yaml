interface:
  display_name: "AutoFigure Edit"
  short_description: "Full AutoFigure pipeline for Codex"
  default_prompt: "Use $autofigure-edit to preserve the full AutoFigure pipeline, and only fall back to direct Codex SVG authoring if I explicitly accept that tradeoff."

policy:
  allow_implicit_invocation: true
